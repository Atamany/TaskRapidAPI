@model UsdTryViewModel

<div class="col-md-4">
    <div class="card">
        <div class="card-header bg-primary text-white">
            <i class="fas fa-dollar-sign me-2"></i>USD/TRY
        </div>
        <div class="card-body stat-card">
            <h3>@Model.data.exchange_rate ₺</h3>
        </div>
    </div>
</div>

